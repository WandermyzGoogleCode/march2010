#summary 手机端测试问题列表

 # 更改密码好像不行
 # 更新BUG
  # 用任意手机号和个人信息，`"test"`为密钥注册；更新为任意信息或任意密钥，正常
  # 用任意手机号和个人信息，非`"test"`为密钥注册；更新为任意信息或任意密钥，服务器返回：
{{{
Call completed, core counter = （不定数值）
}}}
  # 用任意手机号和个人信息，`"test"`为密钥注册；修改为新的密钥（如`"test2"`），重启客户端后，登录，再修改任意信息，服务器返回错误同上。